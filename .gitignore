# Pasta de entrega das imagens. A cópia que o site serve é public/minimaps/.
/minimaps/

node_modules/
.next/
out/
build/
.env
.env.local
.env*.local
# Leading slash matters: a bare `data/` also swallows src/data/, which holds
# the game content the app cannot build without.
/data/
*.db
*.db-shm
*.db-wal
next-env.d.ts
tsconfig.tsbuildinfo
.DS_Store
Thumbs.db
